import styled from "styled-components"
const SliderStyled = styled.div`
    height: 300px;
    background-color: orange;
`
const Slider = () => {
    return <SliderStyled>Slider</SliderStyled>
}

export default Slider